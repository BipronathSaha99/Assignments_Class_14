# >>-------------Factorial value--------------------------------<<
def fact(n):
    sum=1
    i=1
    while i<=n:
        sum=sum*i
        i=i+1
    print(f"Factorial of {n} is = {sum}")
n=int(input("Enter number="))
fact(n)
